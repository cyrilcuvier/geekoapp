[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "geeko-oracle"
version = "0.1.0"
description = "Geeko Oracle - patch status fortune service"
dependencies = [
    "fastapi==0.115.0",
    "uvicorn[standard]==0.30.6",
]
