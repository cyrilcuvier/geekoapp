# Target state: Redis is supplied by the separate AppCo release.
# The Secret is created out-of-band; no credential belongs in this file.
api:
  redisUrl: ""
  redisSecret:
    enabled: true
    name: geeko-redis-auth
    key: url

redis:
  source: appco
