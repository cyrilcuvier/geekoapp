Où est Geeko ? has been deployed.

Redis source: {{ .Values.redis.source }}
{{- if eq .Values.redis.source "dockerhub" }}
  -> deliberately pulled from Docker Hub (session 1 anti-pattern demo).
     Use values-demo-appco.yaml only after the separate AppCo Redis release is ready.
{{- else }}
  -> external Redis expected; REDIS_URL comes from the configured Kubernetes Secret.
{{- end }}

Sage URL: {{ .Values.api.sageUrl | default "(none - session 1, no VM yet)" }}
