apiVersion: v1
kind: Service
metadata:
  name: geeko-frontend
spec:
  type: NodePort
  selector:
    app: geeko-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: {{ .Values.frontend.nodePort }}
